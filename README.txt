To reproduce graphs just run the python script.

Take in mind that the computations take a while.

numpy, matplotlib, pandas, math, operator libraries are needed in order to run the script.

use: "pip install numpy" in cmd line for windows in order to install them if needed.
